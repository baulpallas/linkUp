var router = require("express").Router();

router.get("/", async function(req, res) {
  res.render("event", {});
});

router.get("/", async function(req, res) {
  res.render("event", {});
});

module.exports = router;
